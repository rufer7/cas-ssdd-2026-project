# Disable Microsoft Entra ID auto-configuration during tests so the application
# context can be loaded without configuring a real Entra ID tenant. Tests that
# exercise security use Spring Security Test (e.g. @WithMockUser, jwt() mock).
spring.cloud.azure.active-directory.enabled=false
